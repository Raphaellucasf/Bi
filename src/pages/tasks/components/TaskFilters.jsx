import React from "react";

const TaskFilters = ({ filters, onFilterChange, resultCount }) => (
  <div className="mb-4 p-4 bg-gray-50 border rounded">
    <p className="text-sm text-gray-700">Filtros de tarefa (placeholder)</p>
  </div>
);

export default TaskFilters;
