import React from "react";

const PublicationFilters = ({ filters, onFilterChange, resultCount }) => (
  <div className="mb-4 p-4 bg-gray-50 border rounded">
    <p className="text-sm text-gray-700">Filtros de publicação (placeholder)</p>
  </div>
);

export default PublicationFilters;
