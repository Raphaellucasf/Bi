import React from "react";

const ClientFilters = ({ filters, onFilterChange, resultCount }) => (
  <div className="mb-4 p-4 bg-gray-50 border rounded">
    <p className="text-sm text-gray-700">Filtros de cliente (placeholder)</p>
  </div>
);

export default ClientFilters;
